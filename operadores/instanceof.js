let arr = [1, 2, 3];
let obj = { a: 1, b: 2 };

let ehArray = arr instanceof Array; // Verifica se arr é uma instância de Array (retorna true)
let ehObjeto = obj instanceof Object; // Verifica se obj é uma instância de Object (retorna true)

console.log(ehArray)
console.log(ehObjeto)